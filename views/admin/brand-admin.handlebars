      <!-- End Navbar -->
      <div class="content">
        <div class="container-fluid">
          <div class="content">
        <div class="container-fluid">
          <div class="row">
            <div class="col-md-12">
              <div class="card">
                <div class="card-header card-header-primary">
                  <h4 class="card-title ">Brands</h4>
                   <a role="button" class="btn btn-primary btn-sm" href="/admin/brand/create" style="background-color: white; color: purple">Add a brand</button></a>
                </div>
                <div class="card-body">
                  <div class="table-responsive">
                    <table class="table">
                      <thead class=" text-primary">
                        <th>
                          Name
                        </th>
                        <th>
                          Description
                        </th>
                        <th class="text-right">
                          Action
                        </th>
                      </thead>
                      <tbody>
                        {{#each brands}}
                        <tr>
                            <td>{{this.name}}</td>
                            <td>{{this.description}}</td>
                            <td class="td-actions text-right">
                                <a type="button" href="/admin/brand/update/{{this.id}}" rel="tooltip" class="btn btn-success" title="Edit Category">
                                    <i class="material-icons">build</i>
                                </a>
                           </td>
                          </tr>
                        {{/each}}
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </div>
          <!-- your content here -->
        </div>
      </div>



