      <!-- End Navbar -->
      <div class="content">
        <div class="container-fluid">
          <div class="content">
        <div class="container-fluid">
          <div class="row">
            <div class="col-md-12">
              <div class="card">
                <div class="card-header card-header-primary">
                  <h4 class="card-title ">Blogs</h4>
                  <a role="button" class="btn btn-primary btn-sm" href="/admin/blog/add" style="background-color: white; color: purple">Add a blog</button></a>
                </div>
                <div class="card-body">
                  <div class="table-responsive">
                    <table class="table">
                      <thead class=" text-primary">
                        <th>
                          Blog Name
                        </th>
                        <th>
                          Description
                        </th>
                        <th>
                          URL
                        </th>
                        <th>
                          Image
                        </th>
                        <th class="text-right">
                          Action
                        </th>
                      </thead>
                      <tbody>
                        {{#each rows}}
                        <tr>
                            <td>{{this.name}}</td>
                            <td>{{this.description}}</td>
                            <td>{{this.url}}</td>
                            <td>{{this.pic}}</td>
                            <td class="td-actions text-right">
                                <a type="button" href="/admin/blog/update/{{this.id}}" rel="tooltip" class="btn btn-success" title="Edit Blog">
                                    <i class="material-icons">build</i>
                                </a>
                           </td>
                          </tr>
                        {{/each}}
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </div>
          <!-- your content here -->
        </div>
      </div>





